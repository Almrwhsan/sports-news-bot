name: Test SportScore Multi Team API

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-sportscore-teams:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test SportScore Multi Team API
        run: |
          python test_sportscore_teams.py
